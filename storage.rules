rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default deny. Open up per-path rules as features are built.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
