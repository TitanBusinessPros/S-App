{
  "name": "survival-day",
  "private": true,
  "description": "Monorepo root. Runtime code lives in web/ and functions/ workspaces; this installs shared test tooling into one deduped node_modules tree so mocks and real code always resolve the same module instance.",
  "workspaces": [
    "functions",
    "web"
  ],
  "scripts": {
    "test:functions": "jest --config testing/functions/jest.config.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.16.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions-test": "^3.4.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.0"
  }
}
